name: pages

on:
  push:
    branches: [main]
  schedule:
    - cron: "17 * * * *"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
        with:
          repository: RobertFlexx/sps-core
          path: vendor/core
      - uses: actions/checkout@v4
        with:
          repository: RobertFlexx/sps-extra
          path: vendor/extra
      - name: Build site
        env:
          GH_TOKEN: ${{ github.token }}
          CORE: vendor/core
          EXTRA: vendor/extra
          SITE_PREFIX: /splux-site
        run: sh tools/build-site.sh
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.page.outputs.page_url }}
    steps:
      - id: page
        uses: actions/deploy-pages@v4
